# Local Presentation Coach

Интерактивное демо локального анализатора для подготовки к выступлениям, которое показывает, что **ML-модели можно запускать прямо в браузере пользователя** — без серверов, API-ключей и отправки видео/аудио в облако.

Демо анализирует выступление докладчика в реальном времени: взгляд, позу, жесты, мимику и качество звука. После остановки сессии формирует итоговый отчёт с метриками и рекомендациями. Все вычисления выполняются **локально на устройстве**.

## Применение

| Что происходит | Почему это важно |
|---|---|
| **Три vision-модели работают одновременно** в live-режиме | Доказывает, что браузер — полноценная ML-платформа, а не только UI-слой |
| **Данные не покидают устройство** | Камера и микрофон обрабатываются в памяти браузера; нет upload, нет стриминга на backend |
| **Нулевая инфраструктура** | Не нужны GPU-серверы, очереди, биллинг за inference, DevOps вокруг ML API |
| **Мгновенная обратная связь** | HUD обновляется каждый кадр — latency определяется только локальным inference, а не сетью |
| **Практичный сценарий** | Не абстрактная классификация картинок, а коучинг выступления — понятный и наглядный use case |

Ответ на вопрос «зачем запускать ML на клиенте»: **приватность, скорость, автономность и отсутствие операционных затрат** — при сохранении качества, достаточного для интерактивного приложения.

---

## Функционал

### Live-анализ (в реальном времени)

При нажатии «Запустить live-анализ» приложение:

1. Запрашивает доступ к **камере** (1280×720, фронтальная) и **микрофону**
2. Загружает ML-модели и WASM-рантайм (см. раздел «Модели и загрузка»)
3. Запускает цикл обработки кадров через `requestAnimationFrame`

**Vision (ML, MediaPipe):**

| Сигнал | Модель | Что измеряется |
|---|---|---|
| Взгляд | Face Landmarker | Eye contact: положение радужки и поворот головы относительно камеры |
| Поза | Pose Landmarker Lite | Стабильность осанки: наклон плеч, смещение головы |
| Жесты | Gesture Recognizer | Распознавание жестов рук (👍, ✌, 🖐 и др.) с анимацией-реакцией |
| Активность лица | Face Landmarker | Энергия мимики: открытость рта + движение лица между кадрами |

На видео рисуются оверлеи: рамка лица (фиолетовая) и линия плеч (зелёная).

**Audio (сигнальный анализ, Web Audio API):**

| Сигнал | Метод | Что измеряется |
|---|---|---|
| Шум | RMS по Float32-буферу | Слишком тихо / норма / слишком шумно |
| Качество | Детекция клиппинга | Процент кадров с peak > 0.97, рекомендации по gain |

> Аудио-часть использует классический DSP (не нейросеть). Это осознанный выбор: для уровня громкости и клиппинга ML не нужен, а демо фокусируется на **vision-моделях** как главном доказательстве локального ML.

### Итоговый отчёт (после остановки)

При остановке live-анализа:

1. Агрегируются метрики сессии (`sessionStats`) — проценты времени по каждому сигналу
2. Формируется визуальный отчёт с progress-bar метриками
3. Генерируются **эвристические рекомендации** (`buildSessionRecommendations`) — правила на основе порогов, **без LLM и без облачных API**

Примеры рекомендаций: «взгляд уходил мимо камеры 35% времени», «мимика вялая», «обнаружен клиппинг».

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                        Браузер пользователя                 │
│                                                             │
│  getUserMedia ──► <video> ──► requestAnimationFrame loop    │
│       │                              │                      │
│       │                    ┌─────────┼─────────┐            │
│       │                    ▼         ▼         ▼            │
│       │              FaceLandmarker PoseLandmarker Gesture  │
│       │                    │         │         │            │
│       │                    └─────────┼─────────┘            │
│       │                              ▼                      │
│       │                     HUD + sessionStats              │
│       │                                                     │
│       └──► Web Audio API ──► RMS / clipping ──► HUD       │
│                                                             │
│  [Стоп] ──► summarize() ──► Report + рекомендации          │
└─────────────────────────────────────────────────────────────┘

         ▲ внешние запросы при старте:
         │  WASM-рантайм (jsDelivr, HTTP cache) + веса моделей (только первый раз)
         │  повторные запуски: веса из IndexedDB, inference полностью локально
```

**Стек:** React 19 + Vite 8, `@mediapipe/tasks-vision` 0.10.35.

**Ключевые файлы:**

| Файл | Роль |
|---|---|
| `src/hooks/useVision.js` | Загрузка моделей, inference-цикл, эвристики vision |
| `src/lib/modelCache.js` | Кэширование весов моделей в IndexedDB |
| `src/hooks/useAudio.js` | Web Audio API, анализ микрофона |
| `src/lib/sessionStats.js` | Агрегация метрик за сессию |
| `src/lib/recommendations.js` | Правила для итоговых рекомендаций |
| `src/components/Report.jsx` | Визуализация итогового отчёта |

---

## ML-модели: что, откуда, как запускается

### Используемые модели

Все три модели — из экосистемы **Google MediaPipe Tasks**, формат `.task` (TFLite + метаданные), квантизация **float16**:

| Модель | Назначение | Размер | URL загрузки |
|---|---|---:|---|
| **Face Landmarker** | 478 лендмарков лица, blendshapes, оценка взгляда и мимики | **3.6 МБ** | `storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task` |
| **Pose Landmarker Lite** | Скелет тела (lite-версия для скорости) | **5.5 МБ** | `storage.googleapis.com/mediapipe-models/pose_landmarker/pose_landmarker_lite/float16/1/pose_landmarker_lite.task` |
| **Gesture Recognizer** | Классификация жестов рук (до 2 рук) | **8.0 МБ** | `storage.googleapis.com/mediapipe-models/gesture_recognizer/gesture_recognizer/float16/1/gesture_recognizer.task` |

**Суммарный вес vision-моделей: ~17.1 МБ** (float16). Это единственный «тяжёлый» download при первом запуске — после кэширования повторные визиты не скачивают веса повторно.

### Кэширование моделей

Веса моделей кэшируются в **IndexedDB** браузера (`local-presentation-coach` → store `vision-models`):

1. При старте live-анализа `getModelBuffers()` проверяет IndexedDB по URL каждой модели
2. Если буфер найден — модель инициализируется через `modelAssetBuffer` без сетевого запроса
3. Если буфера нет — модель скачивается один раз, сохраняется в IndexedDB и используется сразу
4. Статус в UI показывает источник: `кэш`, `сеть` или `кэш + сеть` (частичное попадание)

```js
const { buffers, fromCacheCount, totalCount } = await getModelBuffers([
  FACE_MODEL_URL,
  POSE_MODEL_URL,
  GESTURE_MODEL_URL,
])

FaceLandmarker.createFromOptions(resolver, {
  baseOptions: { modelAssetBuffer: buffers[FACE_MODEL_URL], delegate: 'GPU' },
  runningMode: 'VIDEO',
})
```

| Запуск | Что происходит | Сеть |
|---|---|---|
| **Первый** | Скачивание ~17 МБ + сохранение в IndexedDB | ~17 МБ model weights + WASM |
| **Повторный** (тот же браузер) | Чтение из IndexedDB, инициализация из памяти | Только WASM (~несколько МБ, HTTP cache браузера) |
| **Новая вкладка / перезагрузка** | Модели уже в IndexedDB — без повторной загрузки весов | WASM из HTTP cache |

> WASM-рантайм MediaPipe по-прежнему загружается с jsDelivr, но браузер кэширует его через стандартный HTTP cache. Для полного offline-first можно дополнительно положить WASM и `.task`-файлы в `public/`.

### WASM-рантайм

Инференс-движок загружается из npm-пакета через CDN:

```
https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.35/wasm
```

Инициализация в коде (автовыбор backend):

```js
const resolver = await FilesetResolver.forVisionTasks(WASM_FILES_URL)

// WebGL2 доступен → пробуем GPU для всех трёх моделей, иначе CPU
let models
if (isGpuDelegateAvailable()) {
  try {
    models = await createVisionModels(resolver, 'GPU')
  } catch {
    models = await createVisionModels(resolver, 'CPU')
  }
} else {
  models = await createVisionModels(resolver, 'CPU')
}
```

### GPU vs CPU: как именно выполняется inference

| Аспект | Поведение в этом демо |
|---|---|
| **Вычислительный backend** | **GPU (WebGL2)**, если браузер поддерживает WebGL2 и MediaPipe успешно инициализирует delegate; иначе **WASM (CPU)** |
| **Статус в UI** | «Vision-модели: готовы (GPU)» или «Vision-модели: готовы (WASM (CPU))» |
| **Выбор backend** | Проверка `canvas.getContext('webgl2')` → попытка `delegate: 'GPU'` для всех моделей → при ошибке автоматический fallback на `delegate: 'CPU'` |
| **Потоки** | WASM-рантайм MediaPipe использует SIMD и может задействовать несколько потоков через Web Workers (внутри библиотеки) |
| **Режим** | `runningMode: 'VIDEO'` — оптимизирован для последовательных кадров с webcam |
| **Частота** | Один проход трёх моделей на каждый новый кадр видео (`detectForVideo` / `recognizeForVideo`) |

### Что происходит при старте (пошагово)

1. `getModelBuffers()` — читает веса из IndexedDB или скачивает ~17 МБ с Google Cloud Storage
2. `FilesetResolver.forVisionTasks()` — загружает WASM-модули (кэшируются браузером по HTTP)
3. Параллельно инициализируются три модели из `modelAssetBuffer` (без повторного скачивания)
4. Автовыбор backend: GPU (WebGL2) с fallback на CPU
5. `getUserMedia` → видеопоток на `<video>`
6. `requestAnimationFrame` → на каждом кадре три inference-вызова → обновление HUD и статистики

### Приватность и сеть

| Данные | Куда идут |
|---|---|
| Видеопоток с камеры | Только в RAM браузера, в canvas/video element |
| Аудиопоток | Только в AudioContext, никуда не записывается |
| Метрики сессии | `sessionStats` в памяти, сбрасываются при новом старте |
| Внешние запросы | При **первом** запуске: ~17 МБ весов + WASM. При повторных — веса из IndexedDB, WASM из HTTP cache |

---

## Быстрый старт

```bash
npm install
npm run dev
```

Откройте URL из терминала (обычно `http://localhost:5173`), нажмите **«Запустить live-анализ»**, разрешите камеру и микрофон.

```bash
npm run build    # production-сборка
npm run preview  # предпросмотр сборки
```

### Требования

- Современный браузер с поддержкой WebAssembly SIMD, `getUserMedia`, Web Audio API
- Камера и микрофон (для полного опыта)
- Интернет при **первом** запуске (загрузка ~17 МБ весов моделей и WASM); повторные запуски используют IndexedDB

## Ограничения

- WASM-рантайм всё ещё загружается с CDN (кэшируется HTTP); для полного offline-first можно положить WASM и `.task`-файлы в `public/`
- Pose Landmarker **Lite** — компромисс скорость/точность; для продакшена можно взять full-модель
- Рекомендации — эвристики, не LLM; для «умного коуча» можно добавить локальный LLM (WebLLM, Chrome Built-in AI), но это другой уровень сложности